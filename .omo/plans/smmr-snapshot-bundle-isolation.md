# SMMR snapshot bundle isolation plan

- Return defensive Evidence Bundle copies from controller snapshots.
- Add a regression test that mutates a snapshot and verifies controller state is unchanged.
- Update README and record core verification evidence.
