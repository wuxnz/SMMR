# SMMR OpenCode next-skill planning

1. Consume `SmmrRuntimeSession.nextSkill()` when OpenCode creates an SMMR chat
   session.
2. Include the selected skill in the per-session operating policy and startup
   log.
3. Add focused coverage, update README status, and run adapter verification.
