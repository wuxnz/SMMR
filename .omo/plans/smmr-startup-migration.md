# SMMR startup migration wiring

1. Inject the existing SMMR migration primitive into OpenCode startup.
2. Run it once per plugin-module lifetime and log its result.
3. Add deterministic startup coverage and update README migration guidance.
