# SMMR runtime steps plan

1. Expose bounded controller transitions and evidence operations through the
   harness-neutral runtime session.
2. Add explicit permission checks for network, research, and durable memory
   operations before an adapter executes a step.
3. Test disabled, denied, and permitted operations plus state transitions.
4. Update README, run focused tests/typecheck, record evidence, and merge.
