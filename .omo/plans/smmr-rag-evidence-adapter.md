# SMMR RAG evidence adapter plan

- Extend the core Evidence Bundle with bounded retrieval context and source paths.
- Add a RAG conversion function that maps retrieval document kinds into the core bundle sections.
- Add core and RAG regression tests and declare the package dependency.
- Update README and record focused package evidence.
