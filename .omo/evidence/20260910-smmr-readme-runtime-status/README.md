# SMMR README runtime status evidence

## What was tested

- Compared README status claims with the merged controller, runtime-session, model, and OpenCode lifecycle implementations on `origin/dev`.
- Ran `git diff --check`.

## What was observed

- Controller status names validated evidence and atomic completion enforcement.
- OpenCode status names model/tool evidence capture and preserves the remaining specialized-operation boundary.

## Why it is enough

The public status table now reflects the actual merged runtime behavior and remaining work.

## What was omitted

No source behavior changed in this documentation-only revision.
